#include "main.h"
#include <stdio.h>

/**
* add - adds two integers and returns the result
* @a: int to be added
* @b: int to be added
* Return: always 0
*/
int add(int, int);
{
int c;
c = a + b;
return (c);
}
