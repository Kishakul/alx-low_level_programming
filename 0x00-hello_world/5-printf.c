#include <stdio.h>

/**
* main - prints exactly this grammar,
*with proper grammar, but the outcome is a piece of art,
* Return: 0 if its prints properly, non-zero if otherwise
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (10);
}
